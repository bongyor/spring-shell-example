spring:
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:h2:mem:mydb
    username: tesztuser
    password: tesztpass
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
